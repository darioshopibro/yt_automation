# Remotion Visual Router — DEPRECATED

**⚠️ OVAJ SKILL JE UKINUT. Koristi `remotion-planner` umesto njega.**

Segmentacija transcripta i beat-type odluke su sada deo `remotion-planner` skill-a (KORAK 3).

Ako neko pokrene ovaj skill, reci:
> "Visual Router je ukinut. Pokreni `remotion-planner` — on sad radi segmentaciju + beat tipove + sve ostalo."
